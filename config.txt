**Per numeri di default utilizzare 0, per path personalizzati rimuovere gli asterischi (Una opzione per riga, memoria in Bytes)
    **Parametri validi: numberOfFiles, memoryAllocated, numberOfThreads, socketPath, logPath
    numberOfFiles=100
    memoryAllocated=32000000
    numberOfThreads=8
    **socketPath=./mysocketpath/socket
    **logPath=./mylogpath/log.txt
